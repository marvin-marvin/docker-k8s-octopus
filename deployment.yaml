apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  annotations:
    deployment.kubernetes.io/revision: "9"
    description: WAN octopus
  creationTimestamp: null
  generation: 1
  labels:
    k8s-app: octopus
    location: wan
  name: octopus
  selfLink: /apis/extensions/v1beta1/namespaces/lan/deployments/octopus
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      k8s-app: octopus
      location: wan
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      annotations:
        description: WAN octopus
      creationTimestamp: null
      labels:
        k8s-app: octopus
        location: wan
      name: octopus
    spec:
      containers:
      - env:
        - name: tz
          value: Europe/Berlin
        image: marvinmarvin/docker-k8s-octopus
        imagePullPolicy: Always
        name: octopus
        volumeMounts:
        - mountPath: /etc/cron.d
          name: k8s-cfg-octopus
        resources: {}
        securityContext:
          privileged: false
          procMount: Default
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
      volumes:
      - name: k8s-cfg-octopus
        hostPath:
          path: /root/k8s-cfg/octopus
          type: Directory
      dnsPolicy: ClusterFirst
      hostNetwork: false
      nodeSelector:
        location: wan
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30
status: {}
